name: Build hack77.dylib

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y git make curl unzip \
          gcc-multilib g++-multilib \
          libxml2-dev libssl-dev libplist-dev \
          libzip-dev libz-dev perl libio-compress-perl
    
    - name: Install dm.pl
      run: |
        sudo curl -L https://raw.githubusercontent.com/theos/theos/master/bin/dm.pl -o /usr/local/bin/dm.pl
        sudo chmod +x /usr/local/bin/dm.pl
    
    - name: Install Theos and SDK
      run: |
        git clone --depth 1 https://github.com/theos/theos.git theos
        rm -rf theos/bin
        mkdir -p theos/bin
        cp /usr/local/bin/dm.pl theos/bin/dm.pl
        chmod +x theos/bin/dm.pl
        
        # СОЗДАЕМ VENDOR ВРУЧНУЮ (решает проблему)
        mkdir -p theos/vendor/include
        mkdir -p theos/vendor/lib
        touch theos/vendor/include/.placeholder
        touch theos/vendor/lib/.placeholder
        
        echo "THEOS=$(pwd)/theos" >> $GITHUB_ENV
        echo "$(pwd)/theos/bin" >> $GITHUB_PATH
        
        # Скачиваем и распаковываем SDK
        cd theos
        curl -L https://github.com/theos/sdks/archive/master.zip -o sdks.zip
        unzip -q sdks.zip
        mkdir -p sdks
        cp -r sdks-master/*.sdk sdks/ 2>/dev/null || true
        ls -la sdks/
        cd ..
    
    - name: Build
      run: |
        export THEOS=$(pwd)/theos
        export SDKVERSION=14.5
        export IPHONEOS_DEPLOYMENT_TARGET=14.0
        export PATH=$THEOS/bin:$PATH
        
        # Проверяем, что vendor создан
        ls -la $THEOS/vendor/ || echo "Vendor not found"
        make clean || true
        make package || true
        
        find . -name "*.dylib" -exec cp {} ./hack77.dylib \;
        ls -la hack77.dylib || echo "No dylib found"
        
    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: hack77.dylib
        path: hack77.dylib
